<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌐 ১. ইন্টারনেট ও নেটওয়ার্ক পারমিশনসমূহ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- 📁 ২. অল ফাইলস ও স্টোরেজ পারমিশন (Android 11+) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- 🔔 ৩. পুশ নোটিফিকেশন ও ব্যাকগ্রাউন্ড ডাউনলোড সার্ভিস পারমিশন -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- 🎬 ৪. লোকাল মিডিয়া (ভিডিও, অডিও ও ইমেজ) ও মাইক্রোফোন পারমিশন -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <application
        android:name=".DramaFlixApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.MyApplication">

        <!-- 📢 ফায়ারবেস ডিফল্ট নোটিফিকেশন চ্যানেল -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />

        <!-- 🔔 ফায়ারবেস পুশ নোটিফিকেশন সার্ভিস -->
        <service
            android:name=".service.DramaFlixFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 📥 ব্যাকগ্রাউন্ড হাই-স্পিড ভিডিও ডাউনলোড সার্ভিস -->
        <service
            android:name=".service.VideoDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 📂 ফাইল প্রোভাইডার (ভিডিও প্লে ও ইন-অ্যাপ APK ইনস্টল) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 🚀 MainActivity (লঞ্চার + ডিপ-লিঙ্ক + মিডিয়া প্লেয়ার) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">

            <!-- ১. অ্যাপের মেইন লঞ্চার -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 🔔 ২. নোটিফিকেশন অ্যাকশন ফিল্টার -->
            <intent-filter>
                <action android:name="OPEN_DRAMA_ACTION" />
                <action android:name="OPEN_APP_UPDATE" />
                <action android:name="OPEN_COMMUNITY_CHAT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 💬 ৩. কাস্টম ডিপ-লিঙ্ক ফিল্টার (playdramaflix://) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="playdramaflix" />
                <data android:scheme="dramaflix" />
            </intent-filter>

            <!-- ========================================================================= -->
            <!-- 🌐 ৪. Android App Links (শেয়ার করা লিংকে ক্লিক করলে ব্রাউজারের বদলে অ্যাপ ওপেন হবে) -->
            <!-- ========================================================================= -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- HTTPS ডোমেইন -->
                <data android:scheme="https" android:host="playdramaflix.com" />
                <data android:scheme="https" android:host="www.playdramaflix.com" />

                <!-- HTTP ডোমেইন (ফলব্যাক) -->
                <data android:scheme="http" android:host="playdramaflix.com" />
                <data android:scheme="http" android:host="www.playdramaflix.com" />
            </intent-filter>

            <!-- 🎬 ৫. লোকাল ফাইল থেকে ভিডিও বা অডিও ওপেনার -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>

            <!-- 📤 ৬. সেন্ড ও শেয়ার টার্গেট -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>

        </activity>

        <!-- ⚡ StartApp Ads Activities -->
        <activity
            android:name="com.startapp.sdk.adsbase.activities.OverlayActivity"
            android:theme="@android:style/Theme.Translucent"
            android:configChanges="orientation|keyboardHidden|screenSize" />

        <activity
            android:name="com.startapp.sdk.adsbase.activities.FullScreenActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:configChanges="orientation|keyboardHidden|screenSize" />

    </application>
</manifest>
